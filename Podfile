# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'StudyMacApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for StudyMacApp
  pod 'HandyJSON', '~> 5.0.0'

  target 'StudyMacAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'StudyMacAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
